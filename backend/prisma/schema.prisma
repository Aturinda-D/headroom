generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  name   String @db.Char(1)
  email  String @db.Char(1)
  number Int    @id
}

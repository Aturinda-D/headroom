# .env.example - copy to .env and fill with real values

# App
PORT=3000                 # default port the server listens on
NODE_ENV=development      # development|staging|production
TRUST_PROXY=false         # set to 'true' when running behind a proxy/load balancer

# Database (Postgres)
# Use a full connection string. Do NOT commit real credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# DIRECT_URL is optional (e.g., direct DB connection used for migrations)
# DIRECT_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# CORS - comma-separated allow list; avoid '*' when using cookies (credentials)
# Examples: http://localhost:5173,http://localhost:3000,https://app.example.com
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Rate limiting configuration (milliseconds / requests per window)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Supabase / other DB provider settings (commented examples)
# SUPABASE_URL=https://your-supabase-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key

# Notes:
# - Copy this file to .env in development and replace placeholders with real values.
# - NEVER commit a file with real secrets. Ensure .env is ignored by git (add to .gitignore).
# - When using credentials/cookies, make sure CORS_ORIGIN lists exact allowed origins.
